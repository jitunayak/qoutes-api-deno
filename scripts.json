{
  "$schema": "https://deno.land/x/denon@2.5.0/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run bookmarks.api.ts",
      "desc": "run my app.ts file",
      "allow": ["env", "net"],
      "env": { "PORT": "8000" }
    }
  },
  "logger": { "debug": true }
}
